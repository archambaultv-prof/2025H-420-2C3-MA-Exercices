# Créez une classe `Voiture` avec les attributs `constructeur`, `modele` et
# `annee`. Implémentez la méthode __init__ pour initialiser ces attributs.
# Ajouter les méthodes `droite`, `gauche`, `avancer` et `reculer` qui
# affichent respectivement "La voiture tourne à droite", etc.
class Voiture:
    pass


# Créez une classe `Livre` avec les attributs `titre`, `auteur` et un paramètre
# optionnel `annee_publication` (par défaut None). Implémentez la méthode
# __init__ pour initialiser ces attributs. Ajoutez une méthode `marque_page`
# qui prend en paramètre un numéro de page, mémorise ce numéro et affiche
# "Marque page à la page [numero_page]". Ajoutez une méthode
# `reprendre_lecture` qui affiche "Reprise de la lecture à la page
# [numero_page]" si un numéro de page a été mémorisé et "Début de la lecture"
# sinon.
class Livre:
    pass
